# Voting App — GitOps Configuration

Legacy GitOps configuration directory for the voting-app services.

This directory contains exported or reference copies of GitOps manifests.
The canonical GitOps repo is `mochthebest-byte/gitops`.

## Related

- Active GitOps repo: `mochthebest-byte/gitops`
- ArgoCD apps: `argocd-apps/voting-app.yaml`, `argocd-apps/result.yaml`, `argocd-apps/worker.yaml`
