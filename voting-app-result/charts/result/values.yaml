replicaCount: 1

image:
  repository: kind-result
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 81
  targetPort: 80
  protocol: TCP
  name: http

postgresql:
  host: postgresql
  port: 5432
  database: db
  user: vote_user
  existingSecret: postgresql
  secretPasswordKey: password
