apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgresql
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://charts.bitnami.com/bitnami
    targetRevision: 16.x
    chart: postgresql
    helm:
      values: |
        auth:
          database: db
          username: vote_user
          existingSecret: postgresql   # створюється ESO з AWS Secrets Manager /my-app/postgresql
        primary:
          service:
            type: ClusterIP
          persistence:
            storageClass: gp3
            size: 8Gi
  destination:
    server: https://kubernetes.default.svc
    namespace: voting-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
