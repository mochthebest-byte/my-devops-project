apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgresql
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: registry-1.docker.io/bitnamicharts
    targetRevision: 18.7.x
    chart: postgresql
    helm:
      values: |
        # tag: latest removed — let chart version default match 18.7.x
        auth:
          database: db
          username: vote_user
          existingSecret: postgresql
        primary:
          service:
            type: ClusterIP
          persistence:
            storageClass: gp3
            size: 8Gi
  destination:
    server: https://kubernetes.default.svc
    namespace: voting-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
