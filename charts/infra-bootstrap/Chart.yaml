# ═══════════════════════════════════════════════════════════════
#  Helm Chart — Infra Bootstrap
#  ClusterSecretStore, ExternalSecrets (ESO)
#  Призначений для деплою через ArgoCD (ArgoCD Application)
#  Замінює ручний kubectl apply з k8s/
#  TLS — через ACM (cert-manager/ClusterIssuer не потрібні)
# ═══════════════════════════════════════════════════════════════
apiVersion: v2
name: infra-bootstrap
description: Cluster-level infra resources — ESO ClusterSecretStore, external secrets
type: application
version: 1.0.0
appVersion: "1.0"
