{{- if .Values.global.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vault-init.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vault-init.labels" . | nindent 4 }}
{{- end }}
