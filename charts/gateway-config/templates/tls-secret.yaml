{{- if and .Values.enabled .Values.tls.enabled }}
# ═══════════════════════════════════════════════════════════════
#  TLS Secret — НЕ ПОТРІБЕН для Gateway API
# ═══════════════════════════════════════════════════════════════
#  C2 fix: ACM сертифікат прив'язується через
#  LoadBalancerConfiguration.listenerConfigurations[].certificates,
#  а не через порожній Secret з Ingress-анотацією.
#  Цей файл залишено як заглушку — нічого не створює.
# ═══════════════════════════════════════════════════════════════
{{- end }}
